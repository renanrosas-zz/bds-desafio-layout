import { BrowserRouter, Route, Switch } from "react-router-dom";

const Routes = () => (
    <BrowserRouter>
        <Switch>
            <Route path="/" exact>
            </Route>
        </Switch>
    </BrowserRouter>
);

export default Routes;